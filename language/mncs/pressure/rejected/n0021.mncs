mncs 0.10;

// KNOWN-FAILING reproducer for ENG-PRESSURE-0021: rebinding a name in
// the same scope is rejected. See
// language/mncs/pressure/rejected/README.md; do not "fix" this file.
module pressure.rejected.n0021;
fn probe() -> (result: i64) {
    let x: i64 = 1;
    let x: i64 = x + 1;
    return x;
}
