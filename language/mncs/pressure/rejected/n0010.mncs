mncs 0.10;

// KNOWN-FAILING reproducer for ENG-PRESSURE-0010: u64 iteration domain.
// See language/mncs/pressure/rejected/README.md; do not "fix" this file.
module pressure.rejected.n0010;
fn probe() -> (result: i64) {
    let keys: [u64; 4] = [0, 1, 2, 3];
    iterate k over keys carrying acc: i64 = 0 {
        next acc = acc +% 1;
    }
    return acc;
}
